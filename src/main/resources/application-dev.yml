spring:
  datasource:
    driver-className: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/database_forum
    username: root
    password: root
    image: mysql
  jpa:
    properties:
      hibernate:
        show sql: true
        format sql: true
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 2714c06d4207e3
    password: 85b617e113e8d7
    properties:
      mail:
        transport:
          protocol: smtp
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  secret: secret

